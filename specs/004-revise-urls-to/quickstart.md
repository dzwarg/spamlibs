# Quickstart: Testing the Revised URL and Message Model

1. Run the development server.
2. Send a GET request to the root URL `/`.
3. Verify that you receive a "hello world" response.
4. Send a POST request to the webhook URL with a JSON payload that conforms to the `IncomingMessage` data model.
5. Verify that the webhook successfully receives and processes the message.